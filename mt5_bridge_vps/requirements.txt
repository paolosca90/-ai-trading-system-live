# Runtime web
fastapi==0.104.1
uvicorn[standard]==0.24.0

# MT5 bridge
MetaTrader5==5.0.5200

# I/O e conf
python-multipart==0.0.6
python-dotenv==1.0.0
httpx==0.25.2

# Pydantic 2.x (compila pydantic-core se manca wheel per cp313)
pydantic[email]>=2.8.0,<3

# Opzionale: pin minimo per anyio/starlette coerenti con fastapi 0.104.x
anyio>=3.7.1,<4.0.0
starlette>=0.27.0,<0.28.0
typing-extensions>=4.8.0

